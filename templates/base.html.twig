<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Application{% endblock %}</title>

    <!-- Lien vers la feuille de style Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    {% block stylesheets %}{% endblock %}
</head>
<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('project_list') }}">Accueil</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">

            <!-- Authentication Links -->
            {% if app.user %}
                <li class="nav-item">
                <!-- Show Skill link for freelancers only -->
                {% if is_granted('ROLE_FREELANCER') %}
                    <li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('project_list') }}"> Offres </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('user_applications') }}"> Mes candidatures </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_skill_list') }}">Skills</a>
                    </li>
                {% endif %}


                <!-- Show Create Project link for clients only -->
                {% if is_granted('ROLE_CLIENT') %}

                  <li class="nav-item">
                    <a class="nav-link" href="{{ path('project_list') }}">Mes projets</a>
                 </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('project_new') }}">Créer un projet</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>

                </li>
                <li>
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

<!-- Contenu principal -->
<div class="container mt-4">
    {% block body %}{% endblock %}
</div>

<!-- Footer -->
<footer class="bg-dark text-white text-center py-4">
    <p>&copy; 2025 Mon Application. Tous droits réservés.</p>
    <div>
        <a class="text-white" href="{{ path('project_list') }}">Projets</a> |
        <a class="text-white" href="{{ path('project_new') }}">Créer un projet</a>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
